# Context Notes — 결정과 근거

> 다음 세션(사람/에이전트)이 재추론 없이 이어받기 위한 기록. 최신이 아래(세션 2가 하단).

## 2026-07-16 구현 세션

**패키지 이름 `app/` (계획서 §9는 `server/`였음).**
HEAXHub fastapi 스택의 entrypoint가 `uvicorn app.main:app --root-path $ROOT_PATH`로 고정되어 있어
launch.command 오버라이드 없이 그대로 타려면 `app/` 패키지가 필요했다. materialtwin(backend/app)과도 정합.
계획서 부록 C 14에 개정 기록.

**Q1~Q8 전부 기본값 채택 (Phase 0).**
특히 Q8(오류 언어)은 한국어 채택 → D10 개정. materialtwin이 한국어 오류 관례를 적대 리뷰로 확정한
생태계라 정합을 우선했다. Tool docstring도 한국어.

**DNS rebinding Host 검증 비활성화 (mcp_server.py).**
MCP SDK streamable HTTP의 기본 보호가 Host를 검증하는데, HEAXHub 배포 시 Host가 포털 도메인
(hwax.sec.samsung.net)으로 들어와 421이 난다(테스트로 실증). 서버는 127.0.0.1에만 바인드되고
신뢰 경계가 Caddy이므로 보호를 끄는 것이 옳다고 판단. 외부 노출 차단은 loopback 바인드가 담당.

**StreamableHTTPSessionManager는 프로세스당 1회 run 제약.**
FastAPI lifespan에서 mcp.session_manager.run()을 구동하는데 재기동(2회째 lifespan)이 불가.
운영(프로세스당 1기동)에는 영향 없음. 테스트는 module-scope TestClient로 공유.

**E400/E500 유발 테스트 유예.**
E400(SINGULAR_SYSTEM)은 완전 역산이 들어오는 V1 solve_load_response에서, E500(COMPUTE_TIMEOUT)은
타임아웃 집행 구현에서 트리거 경로가 생긴다. MVP 계산은 ms 단위 순수 numpy라 인위 트리거가 불가능.
카탈로그 전수 존재 테스트로 대체(test_catalog_complete).

**HEAXHub manifest 스키마 v2 실검증에서 얻은 규칙.**
`health_check`/`restart_policy`는 최상위가 아니라 `launch` 하위, restart는 `max_attempts`(not max_retries),
`build.type`(python_venv) 필수 — `build.stack`은 스키마 외 필드지만 스택 리졸버가 읽으므로 병기,
`source.ref` 불허. **materialtwin-web 오버레이는 status 누락 + 최상위 health_check/restart_policy +
source.ref + build.type 누락으로 스키마 위반 상태**(스택 기본값 덕에 동작 중) — MaterialTwinWeb 쪽에 전달할 것.

**manifest source는 당분간 local_path(symlink).**
GitHub push 전이라 즉시 동작을 위해 local_path 채택. push 후 git url로 전환(manifest 주석 참조).

**HEAXHub 등록 반영 시점.**
integrations_scanner.scan_integrations()는 uvicorn 기동 lifespan에서 1회 실행. 현재 :4040에 백엔드가
가동 중(API에 Bearer 인증)이며, 살아 있는 포털을 임의 재기동하지 않고 오버레이만 배치해 뒀다.
다음 재기동 또는 admin sync 때 카탈로그에 나타난다.

**단위 변환 검증 방식.**
같은 물리 적층을 SI/SI_mm로 넣어 A(×1e-3), B(×1), D(×1e3), ζ(불변) 관계를 직접 테스트
(test_units_bridge_si_vs_si_mm). areal_mass의 SI_mm 표기는 t/mm²(값이 매우 작음)로 일관성 우선.

**기준 케이스 기대값은 폐형해 수식으로 모듈 내 산출.**
수기 수치 금지 원칙(계획서 §8) 유지 — reference_cases.py가 Q 상수부터 수식으로 계산해 embed.
엔진 경로(qbar/ABD 적분)와는 독립.

## 2026-07-16 완성 세션 (서버 0.2.0)

**hwax 등록을 오버레이 복사 → 심볼릭 링크(in-tree)로 전환한 이유.**
스캐너의 SIF 경로는 `source.url`이 있을 때만 탄다. local_path(url 없음)는 레거시 in-tree 빌더로
떨어지는데, 이는 integration 디렉터리 안에 코드가 있어야 한다(첫 스캔에서
"integrations/.../.venv/bin/uvicorn not found"로 실증). 디렉터리 자체를 리포 심볼릭 링크로 만들면
데모 앱들과 동일한 in-tree 형태가 되고, 빌더가 리포의 .venv를 그대로 재사용한다.
manifest의 오버레이 사본이 사라져 정본이 하나가 되는 부수 이득도 있다.

**스캐너 out-of-band 실행 결정.**
가동 중인 백엔드(:4040)를 재기동하는 대신, 기동 lifespan과 동일한 코드
(`app.db.session.SessionLocal` + `scan_integrations`)를 backend venv로 1회 실행했다.
스캐너는 멱등 reconcile로 설계돼 있어(App/AppVersion 행 조정) 무중단 반영이 가능했다.
결과: 카탈로그 v0.2.0(updated), 빌드 6.1s, 기동 pid/port는 `var/integration_state/laminate_analyzer_mcp.json`.

**Caddy 라우트의 실제 구조(실측)와 401의 의미.**
`forward_auth(GET /api/v1/authz, 4040) → strip_path_prefix → reverse_proxy 127.0.0.1:9117`.
익명 요청 401은 visibility: team 앱의 정상 인증 게이트다. 포털 경유 MCP 접속은
`--header "Authorization: Bearer <token>"`로 인증을 함께 보내야 하며, SSE·Mcp-Session-Id의
포털 통과 확인은 토큰 확보 후 마무리(§16.6 항목 2·4). 앱 직결(9117)로는 /health 200·SSE 정상.

**프리픽스 헬스 프로브 404는 우리 결함이 아님.**
스캐너의 프로브는 `/apps/<id>/health`(프리픽스 포함)를 앱 포트에 직접 친다. heax-demo-fastapi도
동일하게 404가 난다(fastapi 스택 공통 — Caddy가 strip하므로 실경로는 문제없음). 라우트는 정상 등록됨.

**HEAXHub 빌더가 리포 .venv를 재구성함 — test extra가 삭제되는 트레이드오프 (실측 정정).**
빌더는 python3.12 venv를 새로 만들고 `pip install -e .`(본 의존성만)를 수행한다. 심볼릭 링크
방식에서는 워크스페이스 venv == 리포 .venv이므로 **재빌드 때마다 pytest/hypothesis/sympy 등
test extra가 사라진다**. 개발 재개 시 `pip install -e ".[test]"` 재실행 필요(이번 세션에서 실제 발생
→ 재설치 후 103 테스트 통과 재확인). `.mcp.json`의 stdio 경로와 laminate-mcp 스크립트는 유효 유지.

**교훈 — cd 지속성 사고.**
스캐너 실행 시 `cd HEAXHub/backend`가 세션에 지속되어 이후 pytest·git 커밋이 HEAXHub 리포에서
실행되는 사고가 있었다(HEAXHub에 잘못 생성된 커밋은 mixed reset으로 원상 복구 완료, 파일 무손실).
또 `pytest | tail` 파이프가 실패 종료코드를 가려 커밋 게이트가 뚫렸다. 이후 규칙: 타 리포 작업은
서브셸/절대경로로, 검증 게이트에 파이프 금지.

**S5 시나리오에서 얻은 에이전트 팁.**
materialtwin처럼 list를 반환하는 tool은 MCP가 **원소별 content 항목**으로 직렬화한다 —
content[0]만 파싱하면 첫 원소만 얻는다(agent_guide §3에 반영). 실측 SUS201 E=200 GPa로
digital thread(materialtwin:material/1/test/1 → ply source.ref → payload_hash)가 실제로 이어짐을 확인.

**E400·E500 실트리거 확보.**
E400은 solve_load_response의 특이계 경로(영행렬 직접 테스트 + 파이프라인 monkeypatch),
E500은 batch/sensitivity 루프의 시간 예산(config.COMPUTE_TIMEOUT_S) 집행으로 유발 테스트가 생겼다.
v2.4(d)의 유예가 해소됐고, 문헌 벤치마크 1건만 P4 잔여로 남는다.

## 2026-07-16 PAT 세션 (정식 인증 경로)

**HEAXHub에 PAT를 정식 구현한 이유와 설계.**
authz는 쿠키 세션(브라우저 SSO 승계)과 Bearer JWT(TTL 1h)만 받아 헤드리스 MCP 클라이언트가
설 자리가 없었다. GitHub 스타일 PAT를 추가: `heax_pat_` 프리픽스 + sha256 해시만 저장(평문 1회 노출),
만료(무기한 허용)·폐기·last_used(60s 스로틀 — authz가 /apps/* 요청마다 불리므로 쓰기 증폭 방지)·audit.
검증 단일 소스는 pat_service.resolve_user이고 deps.get_current_user와 authz._user_from_request가
프리픽스로 분기한다. 커밋 6221405(기능)·2b72d0c(laminate 통합 등록).

**라이브 반영 절차 기록.**
테이블은 테스트 모듈의 멱등 create_all이 라이브 PG에 생성(그들의 테스트는 실 PG 사용).
백엔드는 기존 기동 방식 그대로 재기동(`bash -c 'set -a; source .env; set +a; cd backend && uvicorn ...'`
nohup, 로그 var/logs/backend.log). 재기동 시 integrations reconcile이 앱들을 자동 복구했다
(materialtwin_web이 9118로 재기동 — 슬러그 주소라 클라이언트 영향 없음, 포트 불변 가정 금지 재확인).

**E2E 증거 (§16.6 2·3·4 완결).**
Bearer PAT로 Caddy(:4180) 경유 — /health 200, 익명 401 유지, initialize 200+SSE+Mcp-Session-Id,
동일 세션 notifications/initialized 202, tools/list가 Tool 11종 반환. E2E PAT는 admin 소유
"laminate-mcp-e2e"(365d) — scratchpad에 1회 출력, 필요 시 DELETE /api/v1/auth/tokens/{id}로 폐기.

**참고.** HEAXHub 작업 트리에 병행 작업으로 보이는 미커밋 `backend/app/api/v1/mcp.py`(untracked)와
`router.py` 수정이 있었다 — 내 커밋에는 포함하지 않음(명시적 파일 목록으로 커밋).

## 2026-07-16 게이트웨이 자동탐지 세션

**"포털 최상단 자동화"의 실체 — 이미 구축돼 있었고, 앱 쪽 opt-in만 비어 있었다.**
HWAXMcpGateway(:9110)에 heax_registry 자동탐지가 가동 중: HEAXHub `/api/v1/mcp/servers`
(병행 작업의 미커밋 mcp.py)를 60초마다 폴링해, manifest에 `mcp.expose: true`를 선언한
published 앱을 백엔드로 자동 합류시키고 **HEAX 서비스 PAT를 중앙 주입**해 Caddy authz를
통과한다(오늘 구현한 PAT가 이 용도로 이미 config에 들어가 있었음). 우리 manifest에 mcp
블록이 없어 레지스트리가 빈 목록이었고, opt-in 추가(0.2.1)+재스캔 후 60초 내
`heax-laminate_analyzer_mcp` 합류(도구 118→129), 게이트웨이 경유 analyze 실호출 ok.

**토큰 3층 구분 (혼동 주의).**
① HWAXPortal PAT(`POST /auth/pat`, RS256+JWKS) — rest_proxy `/api/<site>/*`용.
② GW_TOKEN — MCP 게이트웨이(:9110/mcp) 인바운드 인증, 에이전트 설정에 이것 하나만.
③ HEAXHub PAT(`heax_pat_`, 오늘 구현) — 게이트웨이→HEAX 앱 구간에 중앙 주입(클라이언트 무관).

**materialtwin 주의.** materialtwin MCP는 stdio 전용이라 레지스트리 opt-in 불가.
HTTP transport 추가(§16.4 백로그) 시 같은 manifest 블록으로 자동 합류 가능.

**manifest 스키마 v2에는 mcp 블록이 없다(additionalProperties: false 위반).**
스캐너가 스키마를 강제하지 않아 동작하지만, 병행 작업 정리 시 schema v2에 mcp 블록 추가 권장.

## 2026-08-28 기하 비선형 세션 (서버 0.8.0, 도구 24종, 테스트 243)

**계획서의 Hyer 정식화가 틀렸고, 구현하면서 발견해 교체했다.** §18.2 초안은 Hyer의 다항
가정을 `ε⁰ = (c₁ − ½a²x², c₂ − ½b²y², 0)` 으로 적었다. 코드로 옮겨 돌려 보니 판을 키울수록
곡률이 0으로 수렴하고 **분기가 아예 없었다** — 어떤 크기에서도 안장 하나뿐. 원인은
von Karman 적합조건이었다:

  ε_x,yy + ε_y,xx − γ_xy,xy = w,xy² − w,xx·w,yy = −κx·κy

초안 변형족의 적합 잔차는 **0**이라 필요값 −ab와 어긋난다(sympy로 확인). 결과적으로
**원통에는 있지도 않은 a⁴ 막 벌점을 물리고, 안장에는 아무 벌점도 안 물렸다** — 물리가 정확히
반대였다. 적합조건을 만족하는 최소 다항족

  ε_x⁰ = c₁ − d₁y²,  ε_y⁰ = c₂ + (d₁ − ab/2)x²,  γ_xy⁰ = 0   (c₁, c₂, d₁)

으로 교체하니 즉시 옳은 그림이 나왔다. **ab=0(원통)이면 우변이 0 → 균일 변형이 적합 →
막 벌점 없음**(전개 가능면), ab≠0(안장)이면 L⁴ 벌점. 이게 쌍안정성의 전부다.

교훈: 이번 확장은 폐형해가 아니라 **가정한 변형족의 적합성이 답을 결정**한다. Ritz 부분공간을
정할 때 적합조건 검증을 먼저 하고 코드를 쓸 것.

**탐색 격자 척도도 별도 함정이었다.** 선형 CLT 곡률로 격자를 잡았더니 (틀린 정식화에서)
관심 영역이 격자 한 칸보다 작아 분기를 통째로 놓쳤다(선형 10.2 vs 실제 0.2 [1/m]). 올바른
정식화에서는 b=0일 때 ∂U/∂a가 **정확히 선형**이라(원통은 막 벌점이 없으므로) 두 점으로 근을
정확히 잡아 척도를 정한다. 검증용 스캔을 돌릴 때 "해가 1개뿐"이라는 결과는 **모델이 맞다는
증거가 아니라 격자가 틀렸다는 신호일 수도 있다** — 에너지 지형을 직접 그려 확인했다.

**대처짐 β 계수는 손으로 짐작하지 말고 유도했다.** 초안의 `β = (3/16)(A11p⁴ + A22q⁴)`는
두 군데가 틀렸다(계수 3/16 vs 1/16, A11 vs 1/a11 = A11 − A12²/A22). Airy 함수 적합조건을
풀어 Galerkin 투영한 결과를 sympy로 검증했다:

  β_movable = p⁴/(16a11) + q⁴/(16a22),  β_immovable = β_movable + (A11p⁴+2A12p²q²+A22q⁴)/8

**면내 경계조건이 지배적 가정**이라는 게 이 유도의 핵심 소득이다 — 등방 정사각에서 β가
(3−ν)/(1−ν) ≈ 3.86배 차이난다. 하나만 조용히 고르면 안 되므로 `edge_condition`을 명시
입력으로 뒀다(기본은 보수적인 movable).

**좌굴후 강성비 0.5를 하드코딩하려다 유도로 바꿨다.** 끝단 수축 e = a11·N + W²p²/8 을
미분하면 S = a11/(a11 + p⁴/(8β)). 등방 정사각·movable에서 **정확히 0.5**로 환원되고
(고전값 재현), 면내 비대칭 적층에서는 0.5가 아니다([0/0/0/90] → 0.409). 상수를 박았다면
직교이방 적층에서 조용히 틀렸을 것이다.

**스냅 하중은 일부러 안 만들었다.** 계획서 §18.2에 "스냅 하중"이 적혀 있었지만, 무하중
형상만 푸는 이 도구로는 하중 인가 평형 가지의 한계점을 찾을 수 없다. 에너지 장벽까지만
주고 응답·문서에 "계산하지 않는다"고 명시했다. 그럴듯한 근사식을 지어내는 것보다 낫다.

**검증 기준을 바꿔야 했다.** §17까지는 폐형해라 "문헌값과 몇 % 일치"가 기준이었다. §18은
저차 근사라 그 기준이 성립하지 않는다. 대신 ① 극한 환원(작은 판/작은 하중 → 선형해,
1항 Navier 정확값 4/π⁶와 rtol 1e-12) ② 에너지 감소·안정성(안정해 < 불안정해)
③ 불변식(거울쌍 대칭, 임계 크기 ∝ 두께가 rtol 1e-6로 불변, 부호 반전 홀함수)
④ **독립 경로 교차확인**(연속 추적으로 구한 임계 크기가 전수 스캔의 해 개수 변화 1↔3을
정확히 괄호) 으로 짰다. 테스트 27종.

## 2026-08-28 항목 4 세션 (재료 전단 비선형, 서버 0.9.0, 도구 25종, 테스트 269)

**기하 비선형과 재료 비선형을 한 도구에 섞지 않았다.** §18.2~18.4는 형상이 커서 생기는
기하 비선형이고, 항목 4는 재료 자체의 비선형이다. 물리가 다르고 필요한 입력도 다르며
(판 크기 vs 재료 상수), 무엇보다 **전단 지배 적층에서는 재료 쪽이 훨씬 크게 작용한다**
(실측: [±45]s에 Nx=50 N/mm → Ex가 선형의 61%). 별도 도구로 두고 서로를 가리키게 했다.

**수렴 척도를 τ의 변화량이 아니라 구성식 잔차로 잡았다.** 처음엔 반복간 |Δτ|/τ 를 썼는데,
그건 "반복이 멈췄다"만 말할 뿐 "구성식을 만족한다"를 말하지 않는다. 저완화(0.5)를 쓰면
둘이 갈릴 수 있다. 최종 상태에서 직접

    |γ12 − (τ12/G12 + S6666·τ12³)| / |γ12|

를 재서 반환한다. 실측 잔차는 Nx=1e4~4e5 N/m 전 구간에서 1e-11 이하. 이 값이 곧
"이 답이 정말 Hahn–Tsai 해인가"의 증거라 응답에 그대로 싣는다.

**ply 내 평가점은 |τ12| 최대점.** ply당 할선 G 하나를 쓰므로 구성식은 한 점에서만 정확하다.
중앙면을 쓰면 굽힘 지배 하중에서 연화를 과소평가하는데 **그쪽이 비보수**다(실제보다 뻣뻣하게
보고). 최대점을 쓰면 할선 G가 더 작아 더 무르게 나오므로 보수적이다. 이 선택을 assumptions에
명시했다. 순수 막하중에서는 ply 내 τ가 일정해 차이가 없다(주 사용처인 [±45] 인장이 그렇다).

**[±45]의 Gxy 불변식이 강한 검증이 됐다.** 45°에서 Q̄66 = (Q11+Q22−2Q12)/4 로 **Q66 의존이
정확히 상쇄**된다. 그래서 ply G12를 아무리 낮춰도 적층 Gxy는 변하지 않고, 반대로 Q̄11은
Q66에 강하게 의존해 Ex만 떨어진다. 할선 갱신이 엉뚱한 성분에 들어갔다면 이 비대칭이 깨진다 —
Gxy비 = 1.0(rel 1e-12), Ex비 = 0.61 을 테스트로 고정했다. τ12 = Nx/(2h) 폐형해 일치와 함께
두 개의 독립 검증점이다.

**3차식에 강도 한계가 없다는 게 진짜 위험.** Nx=4e5 N/m 에서 γ12 = 2.6 (260% 전단변형)을
아무 저항 없이 뱉는다 — 실제로는 한참 전에 파손했다. γ12 > 0.05 에 W130을 걸고
recover_ply_stresses 병행을 지시한다. 강도 데이터가 없으면 W120도 함께 붙인다.
"조용히 틀리는 것이 최악"이라는 이 서버 계약의 전형적 사례라 게이트를 두 겹으로 뒀다.

**역게이트도 달았다.** ply에 shear_nonlinear가 있는데 `solve_load_response`(선형)를 쓰면
W130으로 이 도구를 가리킨다. 물성을 넣어 놓고 선형 도구를 쓰는 것이 가장 흔한 실수다.

**단위: S6666은 1/응력³.** SI_mm↔SI 변환 계수가 1e-18 이라 자릿수 착오가 나기 쉽다.
units.py에 `shear_nl`을 추가하고 SI/SI_mm 동일 물리 테스트로 고정했다.

**배포 경로 실측 갱신.** 현재 라이브는 심볼릭 링크 in-tree가 아니라 **GitHub source.url →
SIF 빌드**다(var/sifs/laminate-analyzer-mcp.sif, 인스턴스 127.0.0.1:9152). 따라서 예전 노트의
"재빌드 때마다 리포 .venv의 test extra가 사라진다"는 **더 이상 해당하지 않는다**. 다만 배포는
반드시 push가 선행돼야 한다(빌더가 GitHub에서 clone).

## 2026-08-28 V3 적대 검증 세션 (서버 0.9.1, 확정 13건 수정, 테스트 281)

배포 직전 4개 렌즈(수학·통합·결정론·비보수) 탐색 + 반증 기본값 검증. 후보 23건 중
**확정 13 / 반증 7**. 이전 스프린트들과 같은 결론 — 단위 테스트가 통과하는 코드에서
적대 검증이 매번 실제 비보수 오류를 찾아낸다.

**가장 큰 것은 모델 자체였다 — γxy⁰ = 0 제약.** 나는 Hyer 고전 모델을 따라 면내 전단을
0으로 묶었는데, 이게 안장 가지의 막 벌점을 과대평가해 **없는 쌍안정을 만들어낸다**.
실측: [0/0/90] 이 제약 모델에서는 안정해 2개(쌍안정), 자유도를 풀면 1개(단안정).
γxy⁰ = k·x·y 를 넣으면 적합조건이 ε_y⁰ = c₂ + (d₁ + (k−ab)/2)x² 로 바뀌는데, **중심 원점
사각영역에서 xy 와 {1, x², y²} 의 교차적분이 전부 0**이라 A66·k²·∫(xy)² 벌점으로만 들어간다
(A16/A26 커플링·B16/B26 모멘트항·N6 하중항이 모두 소멸). 그래서 4×4 선형계로 여전히
해석적 소거가 되고 코드는 거의 안 늘었다. sympy로 먼저 확인하고 넣었다.

교훈: **고전 모델을 그대로 따르는 것이 안전하지 않을 수 있다.** Hyer의 원 논문은 대칭
크로스플라이를 다뤘고 거기서는 γxy⁰=0 이 무해하다. 불균형 적층으로 범위를 넓히면서 그
제약을 같이 들고 온 것이 문제였다. Ritz 부분공간은 **적용 범위마다 다시 점검**해야 한다.

**대리지표를 쓴 게 두 번째 큰 실수였다.** 비틀림 표현 불가를 A/B/D 의 16·26 성분비로
판정했는데, 이건 물리량이 아니라 대리지표다. [0/90/45/90](κxy가 κx의 59%)이 **아무 경고
없이** "saddle, 단안정"으로 통과했다. 정작 판정 대상인 κxy 는 이미 손에 있었다 —
`linear_curvatures` 가 6성분 해를 구해놓고 sol[3], sol[4]만 반환하고 sol[5]를 버리고 있었다.
`linear_curvature_vector` 로 3성분을 다 돌려주고 실제 κxy 로 게이트를 건다.

교훈: **판정 대상 물리량이 손에 있으면 대리지표를 쓰지 마라.** 특히 그 대리지표가
"보통은 같이 움직인다" 수준의 상관이면 반드시 반례가 있다.

**"가장 나쁜 경우"를 보고하는지 항상 확인하라.** energy_barrier 를
`min(unstable) − min(stable)` 로 썼는데, 이건 **가장 깊은 우물** 기준이다. 스냅스루는 얕은
우물에서 먼저 일어나므로 실제 위험 장벽은 그보다 작다(최대 19배). 안정형상별 장벽을 모두
싣고 min 을 지배값으로 보고한다. 피로에서 부호를 잃어 가장 가혹한 R=−1 을 놓쳤던 것과
같은 종류의 실수다 — **집계할 때 min/max 방향이 위험 쪽인지 매번 확인할 것.**

**유도를 끝까지 전개하지 않은 것.** 좌굴후 강성비를 R=0 에서만 유도해 놓고 2축 하중에
그대로 썼다. R 까지 전개하면 S = (a11+R·a12)/[(a11+R·a12) + p²(p²+R·q²)/(8β)] 인데,
코드는 분자에서 R·a12 를, 분모에서 R·q² 를 빠뜨렸다. R=1 에서 0.5 대 0.259(1.93배 비보수),
R=2 에서 4.25배. 등방 정사각 R=0 검증값 0.5 에만 맞춰 놓고 통과시킨 셈이다.

교훈: **검증 케이스가 파라미터의 특수값(R=0)이면 일반값에서 틀릴 수 있다.** 폐형해 대조는
가능하면 파라미터를 하나 이상 변화시켜서 할 것.

**Airy 컴플라이언스는 3×3 역행렬의 좌상 2×2 다.** `inv(A[:2,:2])` 로 쓰면 A16/A26 이 있는
불균형 적층에서 β를 과대평가한다 — [30]₄ 에서 β 1.91배, 처짐 0.81배(비보수). 균형 적층에서는
정확히 같아서 기존 테스트가 전부 통과했다.

**폐형해 오라클의 위치를 재정의했다.** §18.2의 정사각 폐형해는 γxy⁰=0 **제약 모델**의 해다.
모델을 고치자 24% 어긋났는데, 이건 오라클이 틀린 게 아니라 **다른 모델을 재는 자**였던 것이다.
A66 → ∞ 로 k=0 을 강제하면 rel 1e-8 로 그대로 일치한다(테스트로 고정). κ_lin(L→0)·κ_∞(L→∞)는
두 모델에서 동일하다 — 평면·원통 극한에는 면내 전단이 개입하지 않기 때문. 출하 모델의 L_crit 은
제약을 풀었으므로 **항상 더 크다**(엄격 부등식으로 테스트). 오라클을 버리지 않고 의미를
좁혀서 살린 것이 중요하다.

**반증된 7건도 기록해 둔다** (다시 제기되면 여기를 볼 것): 좌굴 전 강성비 표기, panel 없는
w/h 게이트 부재, _offaxis_ratio 오탐률, critical_panel 의 1 ULP 단위 불일치, 유효폭의 평균응력
사용. 검증자들이 실행으로 반박했다.

## 2026-08-28 자유 가장자리 박리 (서버 0.10.0, 도구 26종, 테스트 299)

**"더 해볼 확장은?"에 답하기 위해 먼저 공백을 숫자로 증명했다.** 아이디어를 나열하는 대신
실제로 도구를 돌려 "지금 서버가 자신 있게 답하는데 틀릴 수 있는 지점"을 찾았다. 결과:

- `[±45]s`, Nx=30 N/mm → Tsai-Wu **R = 2.075, 경고 0건**. 그런데 각 ply 는 τxy = ±26 MPa 를
  지고 있고 자유 가장자리에서 0이 되어야 한다. 물어볼 도구가 없었다.
- `compute_interlaminar_stresses` 는 횡전단력 {Vx,Vy} 필수 → 순수 면내 인장에서 τxz ≈ 3e-9.
- 결정타: `[±30/90]s`, Nx=150 N/mm, G_Ic = 0.10 N/mm(취성 에폭시 현실값) →
  **면내 R = 1.332 vs 가장자리 여유 0.91.** 면내만 보면 "33% 여유", 실제로는 이미 개시.

**O'Brien(1985) 을 고른 이유.** 자유 가장자리는 본래 특이점을 갖는 3D 문제라 응력장은
폐형해가 없다. 그런데 **에너지방출률은 박리 길이에 무관한 정상상태 값**이라 닫힌 형태가
나온다 — G = (ε²h/2)(E_LAM − E*). 이 서버 계약(결정론·폐형해·FE 불필요)에 정확히 맞는
드문 경우다. 응력장을 흉내내려 했다면 근사의 근사가 됐을 것이다.

**G 만으로는 어느 계면이 뜯기는지 못 정한다 — 이게 설계의 핵심이었다.** G 는 "그 계면이
뜯기면 방출될 에너지"이지 "그 계면에 구동 응력이 있는가"가 아니다. 실제로 `[0/90]s` 는
중앙면(90/90, 재료 불연속 없음)의 G 가 가장 크다. 그래서 계면별 불균형 힘·모멘트를 함께
낸다. 이 분해가 물리를 정확히 가른다:

- `[±45]s` — **σ_y 가 모든 ply 에서 정확히 0** 이라 peel 구동력이 없다. τ_xy 불균형만 남아
  전단 지배. (처음에 σ_y 지표만 만들었다가 [±45] 에서 전부 0이 나와서 알아챘다.)
- `[0/90]s` 중앙면 — σ_y 합력 0, 모멘트만 → peel 지배.
- `[±45]s` 중앙면(−45/−45) — **G > 0 인데 구동력 none**. 재료 불연속이 없어 실제 개시가
  어렵다. 에너지와 구동력을 분리해 보고하는 것이 이 도구의 요점이다.

**ΔE < 0 을 0으로 자르지 않은 것이 중요하다.** 처음엔 `max(0, ...)` 로 클램프했는데,
테스트가 비대칭 `[0/45/−45/90]` 에서 ΔE = −31.6 GPa 를 잡았다. B 커플링으로 온전한 적층의
막강성이 이미 연화돼 있어 갈라진 부분적층이 오히려 더 뻣뻣한 것이다. 0으로 자르면 응답이
**"G=0 → 안전"으로 읽힌다.** 실제로는 모델이 적용 불가라는 뜻이다. `model_valid: false` +
`G: null` + W130 으로 바꿨다. 단일방향처럼 ΔE 가 정확히 0 인 경우만 G=0 이 맞다(구동력 없음).

교훈: **"안전 방향으로 클램프"가 항상 안전한 게 아니다.** 모델 무효와 구동력 없음은 응답에서
반드시 구분돼야 한다. §18의 "조용히 틀리지 않기" 원칙의 또 다른 얼굴이다.

**테스트가 두 번 나를 교정했다.** (1) 위의 ΔE 음수, (2) "면내는 통과하는데 가장자리가 더
빡빡하다"는 주장이 G_c=0.25 에서는 거짓이었다(1.434 > 1.332). G_c 를 현실적 0.10 으로 낮추니
성립했다. 주장을 먼저 쓰고 숫자로 확인하는 순서가 옳았다.

**기준 케이스 R14 는 4×4 대칭 분해로 손유도했다.** [0/90] 2층 부분적층의 막 Ex 는
u=ε0x+ε0y, v=ε0x−ε0y 로 변수변환하면 2×2 두 개로 갈라져 numpy 없이 닫힌 형태가 나온다.
엔진과 소수 4자리까지 일치(38615.9645). §8 의 "수기 수치 금지" 규약을 지킨 독립 경로다.

## 2026-08-28 V4 확장 (서버 0.11.0, 도구 28종, 테스트 327)

사용자의 "더 해볼 확장은?"에 답하려고 **다각도 탐색 워크플로(8 에이전트)** 를 돌리면서,
동시에 내 상위 후보를 숫자로 검증했다. 결과적으로 **탐색이 내 목록보다 중요한 것을 찾았다.**

**내가 짚은 것(전부 실측 확인됨).** 자유 가장자리 박리(별도 커밋), 미시역학 체인 단절,
흡습에 시간 축 없음, 경계조건 SS 하나뿐.

**탐색이 추가로 찾은 것 — 셋 다 "조용한 오답"이고 내 목록보다 값졌다.**

1. **압축 하중의 안정성 침묵 (410배 모순).** `[0/90]s` 0.5mm 에 Nx=−60 N/mm →
   `recover_ply_stresses` 가 R=7.07·경고 0건 으로 "7배 여유". 같은 판 좌굴 여유는 0.0173.
   두 응답 어느 쪽도 상대를 언급하지 않았다. agent_guide 가 강도 판정의 표준 진입점으로
   그 도구를 지목하고 있어 **압축 질문은 구조적으로 이 경로로 들어온다.**
2. **열 구속 좌굴 침묵.** `[±45/0/90]s` 1.0mm·ΔT=+100 → warpage 7.1e-16 mm·경고 0건 →
   "열변형 무시 가능". 그런데 면내 구속 시 ΔT_cr = 24.9 K. **반증에 필요한 N^T 는 이미
   내부에서 계산되고 있었는데 응답에 없었다.**
3. **panel 미지 키 침묵 무시.** `panel={"Lx","Ly","edge_condition":"clamped"}` 와
   `{"Lx","Ly","typo_Lx":999}` 가 둘 다 status=ok 통과. 에이전트는 고정단을 요청했다고
   믿고 단순지지 값을 받는다. 하필 내가 그때 `boundary` 를 구현 중이었다.

교훈: **"무엇을 더 만들까"보다 "지금 무엇을 조용히 틀리고 있나"가 언제나 더 값지다.**
새 물리를 얹는 것보다 기존 도구 사이의 침묵을 메우는 쪽이 실효가 컸다.

**게이트를 설계할 때 오탐을 반드시 확인해야 한다.** `homogenize_layer` 오용 게이트를 처음
강성비 10 으로 걸었더니 **이 도구의 정당한 주용도인 동박층(비 33)이 매번 경고**를 받았다.
입력만으로는 "동일 평면 혼합인가"를 판별할 수 없다(둘 다 등방 물성+체적률로 들어온다).
그래서 용도는 `data["scope"]` 로 **항상** 명시하고 경고는 확실한 경우(비 > 50)에만 걸었다.
같은 이유로 지배모드 게이트는 **인장에서 완전히 침묵**하게 했다 — 기존 응답 바이트 동일 유지를
테스트로 고정했다.

**미시역학은 단일값을 내지 않는 것이 설계의 핵심이었다.** AS4/3501-6 Vf=0.6 에서 E1 은
140 vs 문헌 142 GPa(1.4%)로 좋은데 **G12 는 4.21 vs 7.2 GPa(42% 낮다)**. 단일 추정값만 냈다면
이 도구 자체가 새로운 조용한 오답원이 됐을 것이다. Reuss–Voigt 경계를 항상 함께 싣고
W120 으로 실측 보정을 지시한다. Halpin–Tsai 의 ξ 극한이 정확히 그 경계라는 점(ξ→0=Reuss,
ξ→∞=Voigt, rel 1e-9)이 이 설계를 자연스럽게 만들었다.

**경계조건은 p·q 두 상수로 SS와 고정단이 같은 식을 공유하게 만들었다.**
p = ∫(φ')²/∫φ², q = ∫(φ'')²/∫φ². SS 는 p=(mπ)²·q=(mπ)⁴ 이고 넣으면 기존 Navier 로 rel 1e-12
환원된다 — 이 환원 테스트가 새 경로의 정확성을 통째로 보증한다. CC 는 λ_n 을 고정 이분법으로,
p 를 고정 Simpson 으로 구하는데 **q = λ_n⁴ 항등식이 그 수치적분의 독립 검증**이 됐다(rel 1e-8).
다만 1항 Ritz 는 **상계**라 고정단 N_cr 이 +6.6% 비보수다 — 등방 정사각 k=10.74 vs 정해 10.07.
W130 으로 명시하고 SS 를 하한으로 함께 보라고 안내한다. 진동수는 비 1.829 vs 문헌 1.83 으로 좋다.

**확산은 독립 경로 대조가 깔끔했다.** 급수해와 초기 √t 점근 4√(τ/π) 이 τ ≤ 0.01 에서
rel 1e-9 일치. τ_50 = 0.0492 로 문헌 표준값과 일치. 베이크(탈습)는 수학이 동일해서
`mode` 하나로 붙었는데 PCB 도메인에서는 이쪽이 더 자주 쓰일 질문이다.

**stale 안내도 결함이다.** `compute_interlaminar_stresses` 의 자유단 W130 이 같은 커밋에 있는
`assess_free_edge_delamination` 을 두고 "3D 해석 필요"라고 답하고 있었다. 새 도구를 만들면
**기존 W130 들이 그걸 지목하는지 반드시 훑어야 한다** — 안 그러면 에이전트가 새 능력을
영영 쓰지 않는다.

## 2026-08-28 2순위 1~3번 (서버 0.13.0, 도구 29종, 테스트 360)

탐색 보고서의 2순위를 우선순위 순으로 진행. 세 건 모두 **기존 도구가 이미 선언했거나
원리적으로 못 보던 갭**이었지 새 물리 영역이 아니었다.

**1번 혼합모드 — 확정할 수 있는 곳만 확정한다는 설계.** Suo–Hutchinson 위상각은 수치표라
§17.7.3(수기 표 이식 금지)에 걸린다. 그래서 분할을 포기하는 대신 **대칭 논증으로 확정되는
곳만 확정**했다. 두 부분적층이 거울상이면 축강성이 정확히 같아(rel 1e-12 확인) 상대 미끄러짐이
0 이므로 순수 Mode I 이다 — 근사가 아니라 대칭에서 바로 나온다. 실측으로 거울 분할은 대칭
적층의 중앙면에서만 성립한다. 나머지 계면은 G_Ic~G_IIc 범위로 답하고 대표값은 보수 쪽을 쓴다.
"모르면 범위로 답한다"가 "모르니 안 한다"보다 훨씬 쓸모 있었다.

**2번 열잔류 — 핵심은 물리가 아니라 R 의 의미였다.** 잔류응력은 하중 배수와 함께 커지지
않는다. 비례하중 가정의 tsai_wu 를 그대로 쓰면 잔류분까지 R 배 된다. 파손면 도달 조건을
R 의 2차식으로 풀어야 맞다(tsai_wu_with_offset). 검산으로 R·σm+σr 의 FI 가 정확히 1.0 이
나오는 것을 확인했고, 잔류 0 에서 기존 식과 rel 1e-15 로 일치한다.

**c ≥ 1(잔류만으로 파손) 사건을 버리면 안 된다.** 처음엔 이벤트 선택이 `r > 0` 이라
R=0 을 걸러냈고 ΔT=−200K 에서 이벤트가 통째로 비었다. R=0 은 "하중 없이 이미 파손"이라는
**가장 중요한 결과**다. `r >= 0` 으로 바꾸고 전용 경고를 달았다.

지배 모드도 **파손 시점의 총 응력**(R·σm + σr)으로 판정해야 한다. 기계 응력만 보면 잔류가
지배할 때 모드가 틀린다.

효과가 컸다 — [0/90]s CFRP ΔT=−150K 에서 FPF R 이 1.864 → 0.353 (−81%), 피로 수명은
1.23e8 → 1 회. 90° 층 횡인장 잔류가 Yt 여유를 거의 다 먹기 때문이다. 이걸 빼고 답하던 것이
가장 큰 비보수였다.

**3번 부분합성 — d² 를 항등식에서 역산한 것이 설계의 요점.** 두 면재 중립축 거리 d 를 직접
계산하면 EI_full·EI_layered 와 미세하게 어긋나 극한이 안 닫힌다. EI_full − EI_layered 가
정확히 평행축 항 (EA₁EA₂/(EA₁+EA₂))d² 이므로 **거기서 역산**하면 항상 정합한다. 덕분에
G_c→0 에서 f=0, G_c→∞ 에서 f=1 이 정확히 닫혔다.

**스팬이 필수 입력인 이유를 표로 보여줘야 했다.** 같은 UTG/OCA/UTG 스택이 L=1mm 에서
CLT 22.1배 과대, L=10mm 10.09배, L=200mm 1.03배다. 스팬 없이는 답 자체가 성립하지 않는다.

**PC-01 (적대 검증).** 결합을 선형 `EI_layered + f·ΔEI` 로 썼는데, Newmark ODE 를 직접 풀면
`EI_eff = EI_layered/(1 − r·f)` 인 **조화형**이다. 두 형태는 f=0·f=1 에서 정확히 같아
제가 짠 극한 시험 두 개를 모두 통과했다 — 그래서 중간 구간(2.4~6.1배 비보수)이 안 잡혔다.
교훈은 §18.2 γxy⁰ 때와 같다. **극한 시험만으로는 보간식을 검증할 수 없다.**
이제 중간값을 조화형으로 못 박는 회귀 시험을 따로 둔다.

**교차 게이트는 길이 척도가 있는 곳에만 걸었다.** compute_buckling·compute_natural_frequencies
는 panel 이 있어 짧은 변을 스팬으로 쓸 수 있다. solve_load_response 는 길이가 없어 못 건다 —
없는 척도를 지어내지 않았다. 큰 판·순응층 없는 적층에서 침묵하는 것을 테스트로 고정했다.

**부수 발견: isotropic 재료에는 G13 필드가 없다.** 등방은 G13 = G12 가 물리적으로 맞아
validation 이 이미 그렇게 채우고 있었다. 스키마에 없는 필드를 넣으면 E100 이 난다 —
extra="forbid" 가 제대로 일하고 있다는 신호였다.

## 2026-08-28 임의 경계조건 Ritz (서버 0.14.0, 테스트 383)

**§19.5 의 (p,q) 커널이 그대로 재사용됐다 — 보 함수만 갈아끼우면 됐다.** 좋은 추상화의
값어치를 확인한 사례다. Rayleigh 몫이 p = ∫(φ′)²/∫φ², q = ∫(φ″)²/∫φ² 두 상수로만 쓰여
있었기 때문에 경계조건 확장이 커널 변경 없이 끝났다.

**q = λ⁴ 항등식이 다시 한 번 독립 검증이 됐다.** 부분적분 경계항 [φ″φ′ − φ‴φ] 가
S(φ=φ″=0)·C(φ=φ′=0)·F(φ″=φ‴=0) 어느 조합에서도 0 이므로 성립한다. 형상함수를 새로
6종 유도했는데 전부 이 항등식이 9자리로 맞아 곧바로 신뢰할 수 있었다.

**자유변(F)에서 심각한 함정을 만났다 — 거부가 정답이었다.** 처음엔 S·C·F 6쌍을 다
구현해 CF·FF·SF 형상함수까지 유도했고 q=λ⁴ 도 전부 통과했다. 그런데 문헌 대조에서
SSSF 정사각 좌굴 계수가 k=8.96 이 나왔다 — 문헌은 1.40 이다(**6.4배 비보수**).

원인은 형상함수가 아니라 **모드 선택**이었다. 자유변이 있으면 강체 모드(λ=0: 병진·회전)가
실제 변형을 지배하는데, 1항 Ritz 는 첫 **탄성** 모드를 쓴다. SF 보의 λ₁ = 3.9266 은 SS 의
π=3.1416 보다 **크다** — 덜 구속된 경계인데 더 뻣뻣한 시험함수를 쓰는 셈이라 판을 과도하게
구속한다. p_SF = 27.23 이 p_SS = 9.87 보다 크다는 게 그 신호였다.

수학은 다 맞는데 물리가 틀린 경우다. q=λ⁴ 도 통과했고 고유값도 문헌과 맞았다 —
**단위 검증만으로는 못 잡고 문헌 대조가 잡았다.** §8 의 다중경로 검증 원칙이 또 통했다.

처방은 경고가 아니라 **거부**였다. 6.4배 비보수는 경고로 덮을 수준이 아니고, 사용자가
"경고는 있지만 값은 있으니 참고하자"고 쓸 위험이 크다. E100 으로 막고 이유를 밝혔다.
자유변을 제대로 다루려면 강체 모드를 포함한 다항식 기저 다항 Ritz 가 필요한데 그건
이 커널의 범위 밖이다.

**상계 오차를 표로 측정해 W130 에 그대로 실었다.** SSSS 는 정해(Navier 환원 rel 1e-12),
CCCC +6.6%, SSCC +11.6% — **혼합 경계일수록 커진다.** 1항 Ritz 는 항상 상계라 좌굴에서
비보수다. 진동수는 CCCC 비 1.8293 vs 문헌 1.830 으로 훌륭한데 CSCS 는 +9.0% 다.
"근사입니다"로 뭉개지 않고 측정된 숫자를 주는 게 에이전트에게 훨씬 쓸모 있다.

**CCSS ≠ SSCC 를 테스트로 고정했다.** 4글자 코드에서 축 배정이 실제로 반영되는지는
쉽게 놓칠 수 있는 버그 자리다(mode_pq 에 axis 를 안 넘기면 조용히 x 것만 쓰게 된다).

## 적대 검증 2차 라운드 (2026-08-29, v0.20.0)

후보 32 · 확정 17 · 반증 3. **결함의 성격이 1차와 다르다** — 솔버 수식이 아니라
게이트가 언제 침묵하는가, 도구 사이 체인이 실제로 통하는가에 몰렸다. 도구가 38개로
늘면서 이 표면이 솔버 자체보다 넓어졌다.

**세 유형으로 묶인다.**

① 조용한 실패. GATE-01 은 panel 키가 틀리면 게이트가 return None 해서 warnings=[] 로
나갔다 — panel 을 아예 안 준 것보다 위험하다(사용자는 검사가 돌았다고 믿는다).
NC-03·NC-05·GATE-03 도 같다. 앞으로 게이트를 짤 때는 **"언제 아무 말도 안 하는가"를
먼저 적는다**. 침묵 경로가 곧 결함 목록이다.

② 극한만 맞는 보간식. PC-01 은 §18.2 γxy⁰ 함정과 정확히 같은 유형이었다. 두 극한에서
같으니 제가 짠 극한 시험을 통과했고 중간만 틀렸다. 이제 회귀 시험에 **중간값을 못 박는
항목**을 반드시 넣는다(선형이면 4배 이상 벌어지는 지점 등).

③ 지수 법칙의 보정항. NC-01 의 N_cr ∝ s³ 은 CLT 한정이고 R_s ∝ s² 로 같이 큰다.
"이 법칙은 무엇을 고정했을 때 참인가"를 docstring 에 적지 않으면 다음 사람이 그대로 쓴다.

**검증 방식도 셋으로 정리했다.** 중간값 고정 / 교차 검산(게이트 값을 전용 도구에 직접
넣어 일치 확인 — GATE-03 은 90도 회전 적층 compute_buckling 과 1e-12) / **침묵 시험**
(발화해야 할 때와 침묵해야 할 때를 짝으로 고정 — 경고 피로도 회귀 대상이다).

**검증 중 따로 발견한 것.** compute_buckling 의 headline margin.factor 가 CLT 기준이라
샌드위치에서 4.82 를 내는데 보정값은 2.57 이었다. 보정 인자를 별도 블록에만 두면
에이전트가 합치지 않는다 — 판정용 값은 headline 옆에 나란히 놔야 한다.
